// This component is for creating queues
import React from "react";
import PropTypes from 'prop-types';
import { connect } from 'react-redux';
import { create } from '../../actions/queueActions';

const QueueForm = ({ create }) => {
    const 
}